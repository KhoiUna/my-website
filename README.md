# Portfolio Website Template

1. ```sh git clone https://github.com/KhoiUna/my-website.git && cd my-website```
2. `npm install`
3. `npm run dev` to run Vite
4. Config `./scripts/config.js` to edit displayed text
